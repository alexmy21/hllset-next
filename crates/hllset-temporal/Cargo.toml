[package]
name = "hllset-temporal"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Temporal pyramid: L0–L6 sliding window layers with automatic carry, TF snapshots, and Noether invariant enforcement"

[dependencies]
hllset-core = { workspace = true }
hllset-storage = { workspace = true }

[dev-dependencies]
hllset-dsl = { workspace = true }
